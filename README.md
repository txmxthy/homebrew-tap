# Homebrew tap

Homebrew formulae for `txmxthy` projects.

```sh
brew install txmxthy/tap/nfctl
```

Formulae are generated with each release and updated by release automation. Do
not edit files under `Formula/` manually.
